{
  "name": "get_event_interval",
  "description": "Extracts a date interval for listing calendar events.",
  "parameters": {
    "type": "object",
    "properties": {
      "timeMin": {
        "type": "string",
        "description": "Start of the interval in ISO 8601 format with timezone Europe/Warsaw."
      },
      "timeMax": {
        "type": "string",
        "description": "End of the interval in ISO 8601 format with timezone Europe/Warsaw."
      }
    },
    "required": ["timeMin", "timeMax"]
  }
}
